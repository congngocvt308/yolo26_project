# yolo26_project
Áp dụng mô hình yolo26 để nhận diện vật thể trong phòng 

Để chạy dự án này, bạn chỉ cần tải thư mục về, mở PowerShell Windows tại đây và chạy 2 lệnh: docker build -t yolo26-onnx-pipeline . và docker run -v ${PWD}:/app yolo26-onnx-pipeline
